import { createIconSet, createIconSetFromFontello, createIconSetFromIcoMoon } from 'react-native-vector-icons';
import React from 'react';
import { View, Text } from 'react-native';

const Icon = () => {
    return <View></View>;
};

export default Icon;
